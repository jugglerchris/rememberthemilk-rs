pub struct API {
    api_key: String,
}

impl API {
    pub fn new() -> API {
        unimplemented!()
    }
}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
